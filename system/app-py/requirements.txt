fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.3
python-multipart>=0.0.9
aiofiles>=23.2.1
apscheduler>=3.10.4,<4.0
httpx>=0.27.0
markdown>=3.5
anthropic>=0.40
# Voice activity detection (lightweight — no GPU required)
webrtcvad-wheels>=2.0.14
soundfile>=0.12
# Voice transcription & speaker identification
faster-whisper>=1.0
resemblyzer>=0.1.3
# Optional voice deps (install manually if needed):
#   torchaudio>=2.0        — audio processing (requires torch)
#   pyannote.audio>=4.0    — speaker diarization (requires HF_TOKEN for gated model)
