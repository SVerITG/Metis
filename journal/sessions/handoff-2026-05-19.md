═══════════════════════════════════════════════════
  METIS CONTEXT HANDOFF
  Generated: 2026-05-19 — switching to new computer
═══════════════════════════════════════════════════

## WHO YOU ARE TALKING TO
Stan — Senior Researcher, epidemiology / public health / NTDs / sleeping sickness (HAT).
PhD student (3 articles in progress). Active projects: Metis PKM system (this session),
HAT surveillance research, PhD Article 1. Tools: FastAPI dashboard + MCP server + SQLite.

---

## ACTIVE PROJECTS

| Project | Status | Notes |
|---|---|---|
| Metis Research Cortex v1.0 | **RELEASE READY** — awaiting push + tag | 13 commits ahead of GitHub |
| PH Knowledge DB indexer | **IN PROGRESS** — 46/71 PDFs indexed | Needs resume on new machine |
| HAT Dashboard (Metis_PH) | Separate repo — not touched this session | git remote: metis-ph |

---

## WHAT WAS DONE THIS SESSION (2026-05-18 → 2026-05-19)

### Groups E–L completed (Metis roadmap — all committed locally)

**Group L — Full test suite** (commit 55cad88):
- `tests/conftest.py` — shared `tmp_db` + `db_conn` pytest fixtures
- `tests/unit/test_startup_eval.py` — 16 tests
- `tests/unit/test_db_helpers.py` — 12 tests
- `tests/integration/test_dashboard_routes.py` — all 10 tab routes
- `tests/integration/test_memory_curator.py` — 16 MCP tool tests
- `tests/red_lines/test_red_line_suite.py` — 20 safety/injection tests
- `tests/personas/test_persona_workflows.py` — 10 user personas × full daily workflow
- `pytest.ini`, `system/config/release-checklist.md`, `system/config/release-notes-v1.0.md`
- `README.md` + `CONTRIBUTING.md` — corrected for v1.0 (34 agents, 76+ tools, port 8080)

**Group K — Installer fixes** (commit 2e97be1):
- Fixed GitHub Actions paths (removed stale `metis/` prefix)
- Fixed `install.ps1` ghost path bug
- Added `system/install/tray-launcher.spec` (PyInstaller for MetisTray.exe)

**Earlier groups** — agent upgrades (Career Coach, Learning Coach, PhD Architect,
RC Builder, Cybersecurity, Data Guardian, Memory Curator), startup eval, news freshness
check, plan-change detector, Critic agent.

**Knowledge DB indexer** (commit ab6edc6 + 1 uncommitted):
- `system/install/build_knowledge_db.py` — completely rewritten for WSL stability
  (streaming chunks, gc.collect, SIGALRM timeout, batch=4, WAL recycling, thread limits)
- Just updated with **multi-strategy extraction**: PyMuPDF → pdfplumber → pypdf fallback
  + `_is_valid_pdf()` header check to skip HTML-disguised-as-PDF files
- `system/install/index-ph.sh` — one-line launcher script

---

## OPEN / PENDING TASKS (in priority order)

### 1. COMMIT the PDF extractor update (NOT YET COMMITTED)
```bash
git add system/install/build_knowledge_db.py
git commit -m "PDF extraction: PyMuPDF → pdfplumber → pypdf fallback chain, skip HTML files"
```

### 2. PUSH to GitHub (origin = SVerITG/Metis.git)
```bash
git push origin main
```
13 local commits waiting. SSH was hanging in the previous WSL session — try from new machine.

### 3. CREATE v1.0 tag (triggers GitHub Actions — builds Windows installers)
```bash
git tag v1.0 -m "Metis Research Cortex v1.0"
git push origin v1.0
```

### 4. RESUME the knowledge indexer (46/71 done, re-running skips already-indexed)
```bash
cd "/mnt/c/Users/sverschaeve/OneDrive - ITG/Documents/7. Software/Research Cortex"
bash system/install/index-ph.sh --batch-size 2 --sleep 0.5 --force
```
`--force` re-processes the previously-failed PDFs now that PyMuPDF is the primary extractor.
Use Python from: `/home/sverschaeve/.local/share/metis-mcp/.venv/bin/python3`

### 5. INDEX the epi-methods layer after ph-background completes
```bash
/home/sverschaeve/.local/share/metis-mcp/.venv/bin/python3 \
  system/install/build_knowledge_db.py --database epi-methods --batch-size 2 --sleep 0.5
```

### 6. VERIFY GitHub Actions ran after the v1.0 tag push
- Go to: github.com/SVerITG/Metis → Actions → Build Windows Installer
- Should see Full / Standard / Minimal installer artifacts

### 7. RUN the test suite on the new machine
```bash
pytest tests/unit/ tests/integration/ tests/red_lines/ -v
```

---

## KEY PATHS

| Item | Path |
|---|---|
| Repo root (= METIS_RC_ROOT) | `/mnt/c/Users/sverschaeve/OneDrive - ITG/Documents/7. Software/Research Cortex/` |
| Python for indexer | `/home/sverschaeve/.local/share/metis-mcp/.venv/bin/python3` |
| NTFS venv (broken shebangs — do NOT use pip from here) | `system/mcp-server/.venv/` |
| Dashboard | `system/app-py/` → `bash system/app-py/run.sh` → http://127.0.0.1:8080 |
| MCP server launcher | `/home/sverschaeve/.local/share/metis-mcp/run.sh` |
| SQLite DB | `system/app/data/metis.sqlite` |
| Knowledge indexer | `system/install/build_knowledge_db.py` |
| Indexer launcher | `system/install/index-ph.sh` |

---

## KNOWN ISSUES

- **9 "PDFs" are actually HTML files** — bad downloads from WHO website. They now show
  as `🚫 not a PDF (HTML/XML header)` and are cleanly skipped. Need to re-download:
  `30_WHO-world-health-statistics-2024.pdf`, `62–67_WHO-*.pdf`, `31_WHO-guide-cost-*.pdf`,
  `70_WHO-health-financing-*.pdf`, `72_World-Bank-WDR-1993.pdf`, `74_Wagstaff-*.pdf`

- **SSH push hangs** in some WSL terminal sessions — if `git push origin main` hangs,
  try from a fresh terminal or use the GitHub Desktop app on Windows.

- **metis-ph remote** (`git@github.com:SVerITG/Metis_PH.git`) is the HAT Dashboard —
  a completely separate codebase that does NOT share git history with this repo.
  Nothing to push there from this session.

---

## CONTEXT FOR THIS AI
You are a research assistant continuing work on the Metis Research Cortex — Stan's personal
PKM system built on Claude Code, FastAPI + HTMX, SQLite, and a 76-tool MCP server.
The system is at v1.0 release readiness. The repo is at github.com/SVerITG/Metis.

Stan is switching computers mid-session. The SQLite database and local files are on OneDrive
and will be available on the new machine. The Linux-side venv
(`/home/sverschaeve/.local/share/metis-mcp/.venv/`) will need to be set up fresh if
this is a different WSL instance — run `bash system/mcp-server/setup-mcp.sh` if needed.

Keep responses concise. The priority is: commit → push → tag → run indexer → verify CI.
═══════════════════════════════════════════════════
