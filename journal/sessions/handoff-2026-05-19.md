# Session Handoff — 2026-05-19

## What was completed

### Group L — full test suite (commit 55cad88) ✅
- `tests/conftest.py` — shared `tmp_db` + `db_conn` fixtures (real temp SQLite, METIS_DB env var)
- `tests/unit/test_startup_eval.py` — 16 tests, all 4 startup eval check functions
- `tests/unit/test_db_helpers.py` — 12 tests for db_query / db_scalar / db_execute / get_db_path
- `tests/integration/test_dashboard_routes.py` — 10 tab routes + /health + partials + /static/app.js
- `tests/integration/test_memory_curator.py` — 16 tests for consolidate / surface / health MCP tools
- `tests/red_lines/test_red_line_suite.py` — 20 safety tests (injection probe, SQL safety, route errors)
- `tests/personas/test_persona_workflows.py` — 10 user personas × full daily workflow (~140 parametrized tests)
- `pytest.ini` — test discovery config with markers (slow, e2e, red_line, persona)
- `system/config/release-checklist.md` — 8-section manual v1.0 verification checklist
- `system/config/release-notes-v1.0.md` — full structured release notes (~195 lines)
- `README.md` — v1.0 badge, corrected agent count (34), tool count (76+), port, paths, changelog
- `CONTRIBUTING.md` — corrected clone URL, install paths, skill count

### Knowledge DB indexer — stabilised ✅
`system/install/build_knowledge_db.py` rewritten for WSL stability:
- `OMP_NUM_THREADS=1` and friends set before any import
- Streaming chunk generator (no full-PDF RAM accumulation)
- EMBED_BATCH 32 → 4 (tunable via `--batch-size`)
- Explicit `gc.collect()` between every PDF
- SQLite connection recycled every 20 PDFs (WAL truncated)
- SQLite pragmas: `cache_size=-32000`, `synchronous=NORMAL`
- Per-PDF SIGALRM timeout (default 90 s)
- MemoryError caught at extraction and embedding — skip PDF, not crash
- Max-pages guard (default 400 pages)
- `CPUExecutionProvider` forced for fastembed
- New `system/install/index-ph.sh` one-line launcher

### PH background indexer — partial run ⚠
- Ran: `! bash system/install/index-ph.sh`
- Result: 46/71 PDFs indexed, then OOM-terminated around PDF 53
- Already-indexed PDFs are saved in `pdf_index_state` — re-run skips them
- Remaining ~25 PDFs to index (the larger ones in later folders)
- 9 PDFs failed with `invalid pdf header: b'<!DOC'` — these are HTML files disguised as PDFs (bad downloads), not a script bug

### v1.0 release — NOT yet pushed ⚠
- All code is committed locally (latest: 55cad88)
- `git push origin main` kept SSH-hanging in the terminal session
- v1.0 tag not created yet

---

## First things to do next session

### 1. Resume the indexer with a smaller batch (lower memory pressure)
```bash
! bash system/install/index-ph.sh --batch-size 2 --sleep 1.0
```
Already-indexed PDFs will be skipped automatically. The 9 HTML-disguised-as-PDF files will fail again — that's expected and fine.

### 2. Push to GitHub
```bash
! git push origin main
! git tag v1.0 -m "Metis Research Cortex v1.0"
! git push origin v1.0
```
The v1.0 tag triggers `build-installer.yml` which builds the Windows installers on GitHub's runner.

### 3. Verify GitHub Actions
Go to `Actions → Build Windows Installer` and check that Full / Standard / Minimal installer artifacts appeared.

### 4. Run the test suite
```bash
! pytest tests/unit/ tests/integration/ tests/red_lines/ -v
```
Expected: all pass. If any import-related skips appear, check that `METIS_DB` is set.

### 5. Index epi-methods layer
Edit `system/install/index-ph.sh` to pass `--database epi-methods`, or run:
```bash
! /home/sverschaeve/.local/share/metis-mcp/.venv/bin/python3 system/install/build_knowledge_db.py --database epi-methods --batch-size 2 --sleep 1.0
```

---

## Key facts for next session

| Item | Value |
|---|---|
| Repo root / METIS_RC_ROOT | `/mnt/c/Users/sverschaeve/OneDrive - ITG/Documents/7. Software/Research Cortex/` |
| Python to use for indexer | `/home/sverschaeve/.local/share/metis-mcp/.venv/bin/python3` |
| NTFS venv (broken shebangs) | `system/mcp-server/.venv/` — do NOT use pip from here |
| Dashboard | `system/app-py/` → `bash system/app-py/run.sh` → port 8080 |
| SQLite DB | `system/app/data/metis.sqlite` |
| Latest commit | `55cad88` Add Group L: full test suite, release docs, and v1.0 release prep |
| knowledge.db | `knowledge/knowledge.db` — 0 bytes, unused (indexer writes to metis.sqlite) |
| PDF indexer launcher | `system/install/index-ph.sh` |

## Bad PDFs (skip permanently)
These 9 files are HTML pages saved with a `.pdf` extension — they will always fail:
- `30_WHO-world-health-statistics-2024.pdf`
- `62_WHO-world-health-report-2000.pdf`
- `63_WHO-world-health-report-2010.pdf`
- `64_WHO-everybodys-business.pdf`
- `65_WHO-making-fair-choices-UHC-2014.pdf`
- `66_CSDH-Marmot-closing-gap.pdf`
- `31_WHO-guide-cost-effectiveness.pdf`
- `70_WHO-health-financing-country.pdf`
- `72_World-Bank-WDR-1993.pdf`
- `74_Wagstaff-poverty-and-health-2002.pdf`

These should be re-downloaded as proper PDFs or removed from the library folder.
